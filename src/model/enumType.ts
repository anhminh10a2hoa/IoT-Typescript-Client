export enum Order {
  Ascending,
  Descending
}

export enum Grouping {
  Minute = 'Minute',
  Hour = 'Hour',
  Day = 'Day',
  Week = 'Week',
  Month = 'Month',
  Year = 'Year'
}

export enum DataType {
  NumberType = "number", 
  StringType = "string", 
  LongType = "long",
  BinaryType = "binary",
  BooleanType = "boolean",
}